## 3.1.0-alpha.1 (Minecraft 1.21.1)
[English](changelog.md)/中文

### 新增
- 支持从相邻容器（箱子等）自动抽取有效物品进行处理。
- 产物会优先存入提取机上方相邻的容器中，若无容器则正常弹出为掉落物。
- 平台特定容器集成：Fabric 使用 `ItemStorage`，NeoForge 使用 `Capabilities.ItemHandler`。
- 改进物品输入逻辑，支持批量堆叠插入及模拟操作。

### 变更
- `addItemToInput` 现在返回剩余物品，不再直接修改原堆栈。
- 相邻容器位置会持久化保存，并在重新加载后自动重连。
- 将多处 `level == null` 检查替换为 `assert`，代码更加简洁。
- JEI现在会显示产出概率与最大最小产出量（如果安装的话）

### 修复
- 修复输入槽满时物品插入处理不正确的问题。